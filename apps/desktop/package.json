{
  "name": "@home/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --cwd ../sidecar build:mac && tauri dev",
    "build": "bun run --cwd ../sidecar build:mac && tauri build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.1",
    "typescript": "6.0.3"
  }
}
